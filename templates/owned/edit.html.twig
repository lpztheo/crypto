{% extends 'base.html.twig' %}

{% block title %}Edit Owned{% endblock %}

{% block body %}
    <h1>Edit Owned</h1>

    {{ include('owned/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_owned_index') }}">back to list</a>

    {{ include('owned/_delete_form.html.twig') }}
{% endblock %}
