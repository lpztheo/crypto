{% extends 'base.html.twig' %}

{% block title %}Owned{% endblock %}

{% block body %}
    <h1>Owned</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ owned.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ owned.value }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ owned.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_owned_index') }}">back to list</a>

    <a href="{{ path('app_owned_edit', {'id': owned.id}) }}">edit</a>

    {{ include('owned/_delete_form.html.twig') }}
{% endblock %}
